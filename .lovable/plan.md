Em `src/components/presenca/ListaPresencaPrint.tsx`:

- Linha 138: trocar o `<th>ESCOLA</th>` por `ESCOLA / REGIONAL / REDE` (cabeçalho da coluna da tabela de assinaturas).
- Linha 120: atualizar também o rótulo do bloco de cabeçalho de `Escola/Rede:` para `Escola / Regional / Rede:` para manter consistência.

Nenhuma outra alteração de lógica.